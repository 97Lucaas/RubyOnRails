<%= form_with(model: anniv) do |form| %>
  <% if anniv.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(anniv.errors.count, "error") %> prohibited this anniv from being saved:</h2>

      <ul>
        <% anniv.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container">

  <div class="field">
    <%= form.label :Prénom %>
    <%= form.text_field :name %>
  </div>

  <br/>

  <div class="field">
    <%= form.label :Date_de_naissance %>
    <%= form.date_field :birthdate, as: :date, html5: true %>
  </div>

  <br/>

  <div class="field">
    <%= form.label :Actif %>
    <%= form.check_box :active %>
  </div>

  <br/>

  <div class="actions">
    <%= form.submit "Appliquer" %>
  </div>

  </div>
<% end %>
